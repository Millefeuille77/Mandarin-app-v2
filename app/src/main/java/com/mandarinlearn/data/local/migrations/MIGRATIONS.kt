// MIGRATIONS.kt — Mandarin Learn
// Placeholder for future Room database migrations.
// Phase 2: no migrations needed (version 1). Declare future migrations here as:
//   val MIGRATION_1_2 = object : Migration(1, 2) { override fun migrate(...) { ... } }
// Then add to the MIGRATIONS array below.

package com.mandarinlearn.data.local.migrations

import androidx.room.migration.Migration

/**
 * All Room migrations in ascending version order.
 * Version 1 → initial schema (Phase 2). No migrations yet.
 * Future: MIGRATION_1_2, MIGRATION_2_3, etc.
 */
val MIGRATIONS: Array<Migration> = emptyArray()
